# @africanies/africanies-theme

Framework-independent AFRICANIES theme state, shipping-mode colors, and CSS tokens.

```js
import { ThemeService } from '@africanies/africanies-theme';
import '@africanies/africanies-theme/theme.css';
```

`ThemeService` requires explicit `document` and storage dependencies, keeping server-side imports safe.
